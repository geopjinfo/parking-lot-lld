// Time-unit conversions in milliseconds, shared so durations and fees agree.
const MS_PER_MINUTE = 60 * 1000;
const MS_PER_HOUR = 60 * MS_PER_MINUTE;

module.exports = {
  MS_PER_MINUTE,
  MS_PER_HOUR,
};
